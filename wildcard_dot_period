let exampleStr = "Let's have fun with regular expressions!";
let unRegex = /.un/; // matches any word starting with any letter but ending with "un", dot replaces the letter
let result = unRegex.test(exampleStr);
